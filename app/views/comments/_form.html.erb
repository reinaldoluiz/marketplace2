<%= form_for [commentable, Comment.new] do |f| %>
  <div class="form-group">
    <%= f.text_area :body, class:'form-control', placeholder:'Adicionar comentário'%>
  </div>
  <%= f.submit class:'btn btn-primary'%>
<% end %>